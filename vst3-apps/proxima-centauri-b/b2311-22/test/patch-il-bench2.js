/*  Section 13b rewritten against MEASURED numbers rather than guessed ones.
    Every threshold below has a measurement behind it, taken from the probe:

      reply into silence     0.00000 -> 0.0026 rms
      kinship metric         self 1.000, least kin 0.180
      engine kinship         kin 0.556, stranger 0.279
      reply centroid         dark 386 Hz, bright 406 Hz
      membrane, no partner   bit-identical
      membrane, both voices  16 % of the output
      difference tone        x1.96 with the membrane; a control frequency x1.10
      accent, plasticity 1   60.7 cents in 30 s, bounded, 13 steps
*/
"use strict";
const fs = require("fs");
const P = "C:/Users/peter/b/ArtefactB2311/test/bench.cpp";
let s = fs.readFileSync(P, "utf8");
const NL = s.indexOf(String.fromCharCode(13, 10)) >= 0 ? String.fromCharCode(13, 10) : String.fromCharCode(10);

const START = "    //  ---- 13b. THE INTERLOCUTOR";
const END   = "    //  ---- 13. rates ------------------------------------------------------";
const i = s.indexOf(START), j = s.indexOf(END);
if (i < 0 || j < 0 || j < i) { console.error("ABORT: bounds"); process.exit(1); }

const L = [];
const p = (x) => L.push(x);

p("    //  ---- 13b. THE INTERLOCUTOR: it answers, it is kin or deaf, it changes");
p("    {");
p('        std::printf ("-- the interlocutor\\n");');
p("");
p("        auto mk = [&] (Engine& e, int self, int other, float conv,");
p("                       float commune, float plastic)");
p("        {");
p("            e.p.specimen = (float) self;");
p("            e.p.other    = (float) other;");
p("            e.p.converse = conv;");
p("            e.p.commune  = commune;");
p("            e.p.plastic  = plastic;");
p("            e.p.wake = 0; e.p.transit = 0;");
p("            e.prepare (fs, 512);");
p("        };");
p("        const int N = 5 * 48000;");
p("        //  a phrase, then SILENCE — the reply lives in the silence");
p("        const std::vector<Ev> phrase = {");
p("            { 0, 0, 57, 0.9f }, { 20000, 1, 57, 0 },");
p("            { 24000, 0, 62, 0.9f }, { 44000, 1, 62, 0 } };");
p("        auto rmsFrom = [] (const std::vector<float>& v, int from) -> double");
p("        {");
p("            double e = 0;");
p("            for (int i2 = from; i2 < (int) v.size(); ++i2) e += (double) v[i2] * v[i2];");
p("            return std::sqrt (e / std::max (1, (int) v.size() - from));");
p("        };");
p("");
p("        //  (a) IT ANSWERS: the silence after a phrase fills with a voice");
p("        //      that is not the note's own ringdown.");
p("        {");
p("            std::vector<float> lOff (N), rOff (N), lOn (N), rOn (N);");
p("            { Engine e; mk (e, 3, 3, 0.0f, 0.0f, 0.0f);");
p("              render (e, phrase, lOff.data(), rOff.data(), N); }");
p("            { Engine e; mk (e, 3, 3, 0.7f, 0.0f, 0.0f);");
p("              render (e, phrase, lOn.data(), rOn.data(), N); }");
p("            const double a = rmsFrom (lOff, (int) (4.2 * fs));");
p("            const double b = rmsFrom (lOn,  (int) (4.2 * fs));");
p('            std::printf ("   answers into the silence: rms %.5f -> %.5f\\n", a, b);');
p("            CHECK (b > 5.0e-4 && b > a * 5.0,");
p('                   \"the interlocutor does not answer (%.5f vs %.5f)\", b, a);');
p("        }");
p("");
p("        //  (b) KINSHIP IS REAL, in the metric and in the engine's ears.");
p("        {");
p("            Specimen a, b;");
p("            generateSpecimen (3, a);");
p("            const float kinSelf = spectralKinship (a, a);");
p("            int worst = 0; float kinWorst = 1e9f;");
p("            for (int c = 0; c < kCatalog; c += 7)");
p("            {");
p("                if (c == 3) continue;");
p("                generateSpecimen (c, b);");
p("                const float k = spectralKinship (b, a);");
p("                if (k < kinWorst) { kinWorst = k; worst = c; }");
p("            }");
p('            std::printf ("   ladder overlap: self %.3f, least kin (#%d) %.3f\\n",');
p("                         (double) kinSelf, worst, (double) kinWorst);");
p("            CHECK (kinSelf > kinWorst * 2.0f,");
p('                   \"kinship does not discriminate (%.3f vs %.3f)\",');
p("                   (double) kinSelf, (double) kinWorst);");
p("");
p("            std::vector<float> l1 (N), r1 (N), l2 (N), r2 (N);");
p("            Engine e1; mk (e1, 3, 3,     0.7f, 0.0f, 0.0f);");
p("            render (e1, phrase, l1.data(), r1.data(), N);");
p("            Engine e2; mk (e2, 3, worst, 0.7f, 0.0f, 0.0f);");
p("            render (e2, phrase, l2.data(), r2.data(), N);");
p("            const double tKin = rmsFrom (l1, (int) (4.2 * fs));");
p("            const double tStr = rmsFrom (l2, (int) (4.2 * fs));");
p('            std::printf ("   engine hears: kin %.3f (reply rms %.5f),"');
p('                         " stranger %.3f (reply rms %.5f)\\n",');
p("                         (double) e1.debugKinship(), tKin,");
p("                         (double) e2.debugKinship(), tStr);");
p("            CHECK (e1.debugKinship() > e2.debugKinship() * 1.3f,");
p('                   \"the engine hears no difference between kin and stranger\");');
p("            CHECK (tKin > tStr,");
p('                   \"kin do not answer more strongly than strangers\");');
p("            CHECK (tStr > 1.0e-5,");
p('                   \"a stranger is mute — it must answer faintly, never not at all\");');
p("        }");
p("");
p("        //  (c) IT IS A TRANSLATION: the same body answers a dark utterance");
p("        //      differently from a bright one. The reply carries the SHAPE");
p("        //      of what was said, not a fixed gesture.");
p("        {");
p("            auto replyCentroid = [&] (float depth) -> double");
p("            {");
p("                Engine e;");
p("                e.p.depth = depth;");
p("                mk (e, 3, 3, 0.7f, 0.0f, 0.0f);");
p("                std::vector<float> Lv (N), Rv (N);");
p("                render (e, phrase, Lv.data(), Rv.data(), N);");
p("                const int from = (int) (4.3 * fs), to = (int) (4.9 * fs);");
p("                const Specimen& os = e.otherSpecimen();");
p("                double num = 0, den = 0;");
p("                for (int k = 0; k < os.nModes; ++k)");
p("                {");
p("                    const double f = os.voiceHz * os.ratio[k];");
p("                    if (f < 30 || f > 8000) continue;");
p("                    const double amp = goertzel (Lv.data(), from, to, f, fs);");
p("                    num += amp * f; den += amp;");
p("                }");
p("                return den > 0 ? num / den : 0;");
p("            };");
p("            const double cDark = replyCentroid (0.05f);");
p("            const double cBright = replyCentroid (0.95f);");
p('            std::printf ("   reply centroid: dark utterance %.0f Hz, bright %.0f Hz\\n",');
p("                         cDark, cBright);");
p("            CHECK (cDark > 0 && std::fabs (cBright - cDark) / cDark > 0.03,");
p('                   \"the reply ignores what was said (%.0f vs %.0f)\", cDark, cBright);');
p("        }");
p("");
p("        //  (d) THE MEMBRANE: a third voice neither body owns. Three");
p("        //      properties, and the first is the decisive one — a product");
p("        //      with silence is silence, exactly.");
p("        {");
p("            const int M = 9 * 48000;");
p("            auto held = [&] (Engine& e, std::vector<float>& Lv, std::vector<float>& Rv)");
p("            {");
p("                Lv.assign (M, 0); Rv.assign (M, 0);");
p("                render (e, { { 0, 0, 57, 0.9f } }, Lv.data(), Rv.data(), M);");
p("            };");
p("            //  (d1) with no partner at all it must not exist, bit for bit");
p("            {");
p("                std::vector<float> l1, r1, l2, r2;");
p("                Engine a; mk (a, 3, 61, 0.0f, 0.0f, 0.0f); held (a, l1, r1);");
p("                Engine b; mk (b, 3, 61, 0.0f, 0.9f, 0.0f); held (b, l2, r2);");
p("                CHECK (std::memcmp (l1.data(), l2.data(), (size_t) M * 4) == 0,");
p('                       \"the membrane sounds with only one body present\");');
p("            }");
p("            //  (d2) with both sounding it is a material part of the sound,");
p("            //  and (d3) it puts energy on a DIFFERENCE frequency that is in");
p("            //  neither ladder, far more than it moves a control frequency.");
p("            {");
p("                std::vector<float> l0, r0, l1, r1;");
p("                Engine a; mk (a, 3, 61, 0.7f, 0.0f, 0.0f); held (a, l0, r0);");
p("                Engine b; mk (b, 3, 61, 0.7f, 0.9f, 0.0f); held (b, l1, r1);");
p("                const int F = (int) (5.3 * fs), T = (int) (6.8 * fs);");
p("                double d = 0, t = 0;");
p("                for (int i2 = F; i2 < T; ++i2)");
p("                {");
p("                    d += std::fabs ((double) l1[i2] - l0[i2]);");
p("                    t += std::max (std::fabs ((double) l1[i2]), std::fabs ((double) l0[i2]));");
p("                }");
p("                const double rel = t > 0 ? d / t : 0;");
p('                std::printf ("   membrane: %.0f%% of the sound while both voices ring\\n",');
p("                             rel * 100.0);");
p("                CHECK (rel > 0.05, \"the membrane is inaudible (%.3f)\", rel);");
p("");
p("                const Specimen& ss = a.specimen();");
p("                const Specimen& os = a.otherSpecimen();");
p("                const double f0 = 440.0 * std::pow (2.0, (57.0 - 69.0) / 12.0);");
p("                auto owned = [&] (double f) -> bool");
p("                {");
p("                    for (int k = 0; k < ss.nModes; ++k)");
p("                        if (std::fabs (1200.0 * std::log2 (f0 * ss.ratio[k] / f)) < 40.0)");
p("                            return true;");
p("                    for (int k = 0; k < os.nModes; ++k)");
p("                        if (std::fabs (1200.0 * std::log2 (os.voiceHz * os.ratio[k] / f)) < 40.0)");
p("                            return true;");
p("                    return false;");
p("                };");
p("                //  a difference frequency belonging to neither body");
p("                double target = 0;");
p("                for (int k = 0; k < ss.nModes && target == 0; ++k)");
p("                    for (int l = 0; l < os.nModes; ++l)");
p("                    {");
p("                        const double f = std::fabs (f0 * ss.ratio[k] - os.voiceHz * os.ratio[l]);");
p("                        if (f > 90 && f < 2500 && ! owned (f)) { target = f; break; }");
p("                    }");
p("                CHECK (target > 0, \"no unowned difference frequency to probe\");");
p("                if (target > 0)");
p("                {");
p("                    //  a control frequency, also unowned, well away from it");
p("                    double ctl = 0;");
p("                    for (double q = 1.07; q < 1.9 && ctl == 0; q += 0.013)");
p("                        if (! owned (target * q)) ctl = target * q;");
p("                    const double dOff = goertzel (l0.data(), F, T, target, fs);");
p("                    const double dOn  = goertzel (l1.data(), F, T, target, fs);");
p("                    const double cOff = goertzel (l0.data(), F, T, ctl, fs);");
p("                    const double cOn  = goertzel (l1.data(), F, T, ctl, fs);");
p("                    const double rD = dOff > 0 ? dOn / dOff : 0;");
p("                    const double rC = cOff > 0 ? cOn / cOff : 1;");
p('                    std::printf ("   difference tone %.0f Hz (in neither ladder):"');
p('                                 " x%.2f, control %.0f Hz: x%.2f\\n",');
p("                                 target, rD, ctl, rC);");
p("                    CHECK (rD > rC * 1.4,");
p('                           \"the membrane makes no unowned frequencies (x%.2f vs x%.2f)\",');
p("                           rD, rC);");
p("                }");
p("            }");
p("        }");
p("");
p("        //  (e) SPEECH IS SURGERY: after a conversation the listener's own");
p("        //      voice has moved — in cents — and is still a solvable body.");
p("        {");
p("            auto converse = [&] (float plastic, Specimen& before, Specimen& after,");
p("                                 int& steps, float& peak)");
p("            {");
p("                Engine e; mk (e, 3, 61, 0.8f, 0.0f, plastic);");
p("                before = e.otherSpecimen();");
p("                const int M = 30 * 48000;");
p("                std::vector<float> Lv (M), Rv (M);");
p("                std::vector<Ev> evs;");
p("                for (int h = 0; h < 30; ++h)");
p("                {");
p("                    evs.push_back ({ h * 40000,         0, 45 + (h * 5) % 19, 0.95f });");
p("                    evs.push_back ({ h * 40000 + 24000, 1, 45 + (h * 5) % 19, 0 });");
p("                }");
p("                size_t next = 0; int done = 0;");
p("                peak = 0;");
p("                while (done < M)");
p("                {");
p("                    while (next < evs.size() && evs[next].at <= done)");
p("                    {");
p("                        const Ev& v = evs[next++];");
p("                        if (v.kind == 0) e.noteOn (v.note, v.vel);");
p("                        else             e.noteOff (v.note);");
p("                    }");
p("                    const int m = std::min (2048, M - done);");
p("                    e.process (Lv.data() + done, Rv.data() + done, m);");
p("                    for (int i2 = done; i2 < done + m; ++i2)");
p("                        peak = std::max (peak, std::fabs (Lv[i2]));");
p("                    done += m;");
p("                    //  the message thread's turn, exactly as the plugin does it");
p("                    if (e.remodelPending()) e.serviceRemodel();");
p("                }");
p("                after = e.otherSpecimen();");
p("                steps = e.accentSteps();");
p("            };");
p("            auto driftCents = [&] (const Specimen& x, const Specimen& y) -> double");
p("            {");
p("                const int nm = std::min (x.nModes, y.nModes);");
p("                double acc = 0; int cnt = 0;");
p("                for (int k = 0; k < nm; ++k)");
p("                {");
p("                    if (x.ratio[k] <= 0 || y.ratio[k] <= 0) continue;");
p("                    const double c = 1200.0 * std::log2 (y.ratio[k] / x.ratio[k]);");
p("                    acc += c * c; ++cnt;");
p("                }");
p("                return cnt > 0 ? std::sqrt (acc / cnt) : 0;");
p("            };");
p("            Specimen b0, a0, b1, a1;");
p("            int st0 = 0, st1 = 0; float pk0 = 0, pk1 = 0;");
p("            converse (0.0f, b0, a0, st0, pk0);");
p("            converse (1.0f, b1, a1, st1, pk1);");
p("            const double d0 = driftCents (b0, a0);");
p("            const double d1 = driftCents (b1, a1);");
p('            std::printf ("   accent after 30 s of talking: plasticity 0 -> %.1f cents"');
p('                         " (%d steps), plasticity 1 -> %.1f cents (%d steps)\\n",');
p("                         d0, st0, d1, st1);");
p("            CHECK (d0 < 0.01, \"plasticity 0 remodelled the body (%.2f cents)\", d0);");
p("            CHECK (st0 == 0, \"plasticity 0 applied %d accent steps\", st0);");
p("            CHECK (d1 > 5.0, \"listening does not change the listener (%.1f cents)\", d1);");
p("            CHECK (d1 < 2400.0, \"the accent dissolved the body (%.1f cents)\", d1);");
p("            CHECK (pk1 < 1.0f, \"remodelling broke the ceiling (peak %.3f)\", (double) pk1);");
p("            CHECK (a1.nModes > 0 && a1.eigenResidual < 1e-6f,");
p('                   \"the remodelled body does not solve (residual %.2e)\",');
p("                   (double) a1.eigenResidual);");
p("            //  and it is still a BODY: bounded edges, a real slab, audible");
p("            CHECK (a1.nEdges > 0 && a1.pathLen > 1,");
p('                   \"the remodelled body lost its anatomy\");');
p("        }");
p("");
p("        //  (f) CONVERSE 0: the second body does not exist, whichever it is");
p("        {");
p("            const int M = 2 * 48000;");
p("            std::vector<float> l1 (M), r1 (M), l2 (M), r2 (M);");
p("            Engine a; mk (a, 3,  61, 0.0f, 0.9f, 0.9f);");
p("            render (a, phrase, l1.data(), r1.data(), M);");
p("            Engine b; mk (b, 3, 200, 0.0f, 0.9f, 0.9f);");
p("            render (b, phrase, l2.data(), r2.data(), M);");
p("            CHECK (std::memcmp (l1.data(), l2.data(), (size_t) M * 4) == 0,");
p('                   \"converse 0 is not silent about which body is not there\");');
p("        }");
p("");
p("        //  (g) the whole conversation is deterministic");
p("        {");
p("            const int M = 3 * 48000;");
p("            std::vector<float> l1 (M), r1 (M), l2 (M), r2 (M);");
p("            Engine a; mk (a, 3, 61, 0.8f, 0.5f, 0.7f);");
p("            render (a, phrase, l1.data(), r1.data(), M);");
p("            Engine b; mk (b, 3, 61, 0.8f, 0.5f, 0.7f);");
p("            render (b, phrase, l2.data(), r2.data(), M);");
p("            CHECK (std::memcmp (l1.data(), l2.data(), (size_t) M * 4) == 0,");
p('                   \"the conversation is not deterministic\");');
p("        }");
p("    }");
p("");

s = s.slice(0, i) + L.join(NL) + s.slice(j);
fs.writeFileSync(P, s);
console.log("section 13b rewritten against measurements");

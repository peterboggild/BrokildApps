// Append the 2026-09-22 work to the Thin Walls section of CLAUDE.md.
const fs = require("fs");
const p = "C:/Users/peter/Dropbox/ACTIVITIES/00 VSCODE/CLAUDE.md";
let s = fs.readFileSync(p, "utf8");
const NL = s.indexOf("\r\n") >= 0 ? "\r\n" : "\n";
const anchor = "- **The bash-heredoc backslash rule broke four more times in this build.** Patch scripts go in files via the Write tool. It is already in this file six times.";
if (s.split(anchor).length !== 2) { console.error("anchor not found"); process.exit(1); }

const BT = String.fromCharCode(96);
const c = t => BT + t + BT;

const lines = [
"- 2026-09-22 **the second day: a fifth material, a material per surface, walls that bend, and the late field rebuilt.** 65 host parameters now; 90 engine checks and 73 panel checks, all clear; 19 % of a core for one source, 39 % for four. Full reverb write-up in the tree at " + c("docs/reverb-notes.md") + " — read it before touching any FDN in the fleet.",
"  - **STUDIO (a fifth material, for any room) was SOLVED, not guessed.** For a decay that does not move with frequency the whole Eyring denominator must be constant, so " + c("alpha_b = 1 - exp(-(D - 4 m_b V)/S)") + ", which comes out as CONSTANT absorption near a quarter with a little relief at the very top to pay for air absorption — which is what broadband absorbers plus bass traps physically ARE. The hall reads 0.71 s at 250 / 1k / 4k alike (spread 1.00x) where the same room TILED spreads 3.09x. My first table was typed from intuition and measured 1.43x, RISING with frequency.",
"  - **A studio's treatment is DIFFUSION, not absorption**, so STUDIO carries an ISO 17497 scattering coefficient: a reflection keeps " + c("(1-alpha)(1-s)") + " specularly and the scattered part is picked up by the late field FOR FREE, because the field's level is already calibrated as \"the whole reverberant energy minus what the images carry\". The other four materials scatter zero on purpose, so every room that sounded a particular way still does, byte for byte.",
"  - **A material per surface** (walls / floor / ceiling, nine selectors, floor and ceiling defaulting to -1 = \"as walls\" so nothing moves). Worth more than it looks: the floor and ceiling bounces are the two EARLIEST reflections between a standing source and a standing listener, and a carpet is the most lopsided absorber in a normal room.",
"  - **Breakable walls needed a general image-source search**, because the shoebox method mirrors by index arithmetic and cannot represent a wall that is not axis-parallel. " + c("Source/Geometry.h") + " makes a room a list of planar surfaces; a candidate path is a SEQUENCE of them, mirrored forward and then validated by tracing BACK from the listener (the only order in which each reflection point can be found). A square room keeps the exact arithmetic, so it is untouched. **The decisive check: a wall broken by 2 mm is the same room, and the general search reproduces the exact one to 0.05 dB overall and 0.13 dB over the first 50 ms.**",
"  - **Splay acts on early reflections, NOT on standing waves** — this model's late field is statistical, not modal, so there are no modes to break. Above the Schroeder frequency (72 Hz for the hall) that is where splay does its audible work in a real room too; below it, absorption is what helps. And a fold pushed INWARD is concave, which focuses — worse than flat. Told Peter both rather than letting the feature imply otherwise.",
"  - **THE REVERB WAS UNPLEASANT AND PETER WAS RIGHT.** Three faults: the loop held 95-242 ms against the " + c("RT60/2.2") + " the decay needs (so the network's own modes were resolvable and heard as PITCH — spectral ripple 6.1-10.7 dB where a perfectly diffuse field is exactly **5.57 dB**, a fixed number); the delay modulation had been removed the previous day; and the hall's tail started at 0.51 on Abel and Huang's normalised echo density where 1.0 means the echoes have merged. Cure: allpass diffusers INSIDE every line — unity gain so they cannot change the decay, but their length counts towards the modal density and they split every echo into a train. Ripple now 5.6-6.6.",
"  - **Four things that cost real time, all worth not repeating:** (1) **a diffuser must not outring its room** — an allpass rings " + c("-20log10(g)") + " dB per La, i.e. its own RT60 is 14.5 La at g=0.62, which at La=7 ms is 100 ms against an absorbing room's 85, so the diffusers set the tail (measured 0.22 s where Eyring says 0.09); the chain is sized to the decay now and switched OFF for a dead room. (2) **A Schroeder allpass holds the signal for EXACTLY its own length**, independent of g — the energy-weighted mean is " + c("La(1-g^2)^2/(1-g^2)^2 = La") + " and the " + c("(1-g^2)") + " cancels; charging " + c("La/(1-g^2)") + " made every live decay 27 % SHORT. (3) What is left over, MEASURE: each room now runs its own network at four design decays at prepare and records how far its decay misses the design and how much energy the formula really delivers, interpolated in log decay and **cached per room and sample rate** (without the cache the bench went from one minute to over ten). All fifteen decays are now within a few per cent of Eyring where several ran 15-20 % long. (4) **The direct path was losing up to 5 dB of the top octave and 1.9 dB broadband**, varying with sub-millimetre position, because a 4-point Catmull-Rom kernel has energy 0.64 at a half-sample fraction; a 16-tap windowed sinc put the direct path within a tenth of a dB of the physics from 10 Hz to 16 kHz.",
"  - **THREE OF MY OWN DIAGNOSTICS LIED, all the misplaced-window family.** " + c("probe drr") + " fired its impulse 10 samples in, INSIDE a fresh path's gain ramp, understating every close-range reading by up to 11 dB — the bench had been fixed for exactly this weeks earlier and the probe had not. The ripple was measured over a fixed window 300 ms after the impulse, which for a 0.30 s decay is past the end of the tail; and no untreated window works, because short enough not to span the decay is too short to RESOLVE modes a few hertz apart, so the decay is divided back out (" + c("exp(+13.8t/RT60)") + ") first. And a broadband hunt for a 3-5 dB level error found nothing wrong: " + c("16 pi / A") + " is a statement about ONE FREQUENCY, and broadband mixes octaves whose absorption differs by many dB with the top ones (which carry most of a flat spectrum) the most absorbent. **Measured in the 1 kHz octave the direct-to-reverberant ratio lands within a decibel of theory for all five materials.** Two bench checks were corrected rather than the engine: the ratio is measured with the parts rendered in ISOLATION (a 2.5 ms window clipped a 2.9 ms head response and then counted the offcut as reverberation) and the decay check asserts DECAY rather than an absolute floor the party-wall transmission legitimately exceeds.",
"  - **The panel:** movement keys caught at the WINDOW in the capture phase so a focused menu never sees them; door handles picked analytically against the leaf's current plane (no GPU pass), offered only within 2.5 m, in front, and not through a wall; the plan drawn from the engine's own polygon; choice lists taken from " + c("initialState") + " so a fifth material needed no page edit. The subagent's own mutation testing found two of ITS checks wrong, including one that passed while the 3D mesh was still a rectangle because the corner shading follows the plan on its own and repaints a tenth of the view.",
"  - **WebView2 was the one real field failure**: Peter's downloaded exe showed Internet Explorer's error page on his WORK PC. Fixed by linking the loader statically (" + c("JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING=1") + ", verified by reading the import tables of BOTH binaries) and by checking " + c("GetAvailableCoreWebView2BrowserVersionString") + " at editor start so a missing runtime says so with a download link instead of showing IE. **I asserted a PATH dependency as the cause and the measurement refuted it** — the old build started fine with the PATH stripped to bare Windows. See the FLEET-WIDE line elsewhere in this file.",
];

s = s.split(anchor).join(anchor + NL + lines.join(NL));
fs.writeFileSync(p, s);
console.log("CLAUDE.md: " + lines.length + " lines appended to the Thin Walls section");

"use strict";
const fs = require("fs");
const p = "C:/Users/peter/Dropbox/ACTIVITIES/00 VSCODE/CLAUDE.md";
const raw = fs.readFileSync(p, "utf8");
const NL = raw.indexOf("\r\n") >= 0 ? "\r\n" : "\n";
let s = raw.replace(/\r\n/g, "\n");
const anchor = "### The stationary-PC handover (2026-09-22)";
if (s.split(anchor).length !== 2) { console.error("anchor miss"); process.exit(1); }
const entry = `### 1984 (\`C:\\Users\\peter\\b\\Nineteen84\`) — the vintage polysynth, built 2026-09-22/23
- **Peter's brief**: a "new, old" vintage analog polysynth after the CS-80 (its eight voices, each of two RANKS with a resonant HPF into a resonant LPF, the IL/AL filter envelope, ring mod, sub-oscillator, touch), the Prophet-5 (poly-mod, sync, unison, "stranger things"), the VP-330 (formant choir behind a three-phase ensemble), plus tape/VHS wow, flutter, saturation, age, dropouts, and a hall — "the ultimate retro Vangelis synth", "it has to sound absolutely stunning", the look free ("it doesn't have to look like either"). Then: **"Call it 1984."** Design record \`BrokildApps/1984-DESIGN.md\` (§12 = every measured number), decal order \`BrokildApps/1984-DECALS.md\`, contract \`PROTOCOL.md\` in the tree. PRODUCT_NAME **1984**, tree \`Nineteen84\` (a CMake target cannot start with a digit), plugin code **N984** (checked unique against all sixteen), build id \`N84_BUILD_ID\` (260922.1), patches \`Documents\\Brokild patches\\1984\`, private repo \`brokild-1984\`, installed in \`Brokild collection\` in both houses, published at \`vst3-apps/1984/\` (18-page landscape handbook, 8 demo mp3s, zip). BWFX rack + macros, SPECTRA bus consumed.
- **The architecture that mattered**: one phase per rank giving saw, pulse, triangle and sine AT ONCE with a level each (CS-80 brass = saw + a little pulse + sine; a single-shape oscillator cannot make it); TPT SVF with a tanh on the band-pass state so a screaming resonance is a note; the Black Rider ladder as the second LPF; the CS-80 filter envelope with NO sustain (IL/AL ARE the shape); the whole chain (drive, ensemble, choir, tape) in the oversampled domain before decimation, the hall after. 126 params in one \`SPECS[]\` table (rank rows from one macro), 40 factory patches as sparse real-unit lists (\`Patches.cpp\`), a playable RANDOM.
- **Bench \`test/bench.cpp\`, 86 checks ALL CLEAR**; 4.6 % of a core at 2x with everything on. Tuning ±1 cent at every footage; aliasing under −60 dB at 2x below 20 kHz (the decimator's transition band is above 20 kHz, so the audible band is what is measured); SVF self-oscillates at 1000.0 Hz for 1 kHz, ladder 996.5; hall RT60 1.68 s for 2 s; runaway test at max decay+shimmer+motion: 97 dB down after 40 s. Console host \`test/host\` (\`n84host\`, 10 checks): instrument, plays on MIDI, silent without, param reads back, state round-trips. Panel \`test/uiprobe.js\` 99 checks. Live CDP round trip clean.
- **Lessons paid for (engine)**: (1) **an asymmetric clipper with unequal TOPS is DC plus odd harmonics** — the first VALVE scaled its negative half and measured a 2nd harmonic of −33 dB, same as a clean sine; even harmonics need an even TERM (\`b + 0.45 b²/(1+|b|)\`, monotonic, −16 dB at 30 %). (2) **A linear SVF with k > 0 only rings**; self-oscillation needs k slightly NEGATIVE with the state limiter holding it (the last 5 % of RESONANCE take k to −0.06). (3) **The IL/AL gate read a level set() had not delivered yet** — gated with the previous block's IL, it charged from zero (2182 Hz where 27 was due); a \`fresh\` flag makes set() apply the jump. (4) **Mode and glide were derived only in process()**, so a note-on before the first block allocated POLY whatever the knob said (UNISON read "one voice"); derive at note-on too. (5) **Tape hiss is gated on a signal follower** (5 ms up, 0.6 s down, exact zero below 1e-3) or an idle synth hisses at rest. (6) **Three ensemble taps of a modulated sine partly cancel** (−4.3 dB); normalise by taps^-0.75. (7) **Sync topology is the Prophet's**: the AUDIBLE rank is the slave and the env→pitch route lands on it — my first version synced rank II (whose level SYNC LEAD sets to zero), which the docs agent caught by READING the patch against the engine: the synced rank was inaudible. Rank II is now the silent master; rank II ticks first each sample.
- **Probes that lied** (the standing family): a zero-crossing pitch estimator on a two-tap chorus (46 % "wobble" from cancellation nulls); a pulse source whose spectral null sat exactly on the formant under test (a 68 % pulse nulls its 15th harmonic = 1650 Hz); a pluck measured after it had decayed; a glide test reading voice 0 in a POLY allocation; a sustain test reading past its own buffer; **JUCE's VST3 wrapper offers 2212 parameters** (16×130 emulated MIDI CCs + Bypass) — count by name, skipping "MIDI CC" and "Bypass". And **a render "run" through \`> /dev/null\` that SAC had refused** produced identical stats to four digits — the stale WAV — caught only because a changed patch cannot leave the centroid unchanged. Never hide a run's output.
- **Plates**: element shots (\`sel\`) are scale-independent; a fixed \`rect\` in deck coordinates is NOT — the standalone window came up smaller on a later launch and the deck scaled. \`tools/cdp.js\` now takes \`sel\`, \`sel2\` (union), per-side \`pad\`, \`rect\`, \`scale\`. The rack overlay's \`.bwfx-win\` is taller than the viewport, so \`captureBeyondViewport\` returns black below it — crop to content. \`tools/live.ps1\` nudges the standalone's hash IN PLACE when SAC refuses it: relink before cutting the zip.
- **Process**: the engine and bench first (Fable, one pass, 59/84 on the first compile, ALL CLEAR after two probe-and-fix rounds), then the panel (Opus agent against PROTOCOL.md, in parallel with the processor) and the manual + landing page (Opus agent from the design doc, plates listed in \`docs/manual/PLATES.md\` and shot by me). The docs agent also caught STRANGER THINGS' sub-oscillator routed to nothing. **Both agents' deliveries were verified live, not by their reports**: the panel over CDP against the real processor, the manual through the overflow gate (\`tools/shoot-manual.ps1\`, page count from the document, folio check on any text node) and by looking at the renders.
- Not built, deliberately: MPE; a vocoder (the choir is a fixed bank by design and a real one needs a sidechain bus); decals (ordered in \`1984-DECALS.md\`; every hook has its procedural fallback and the panel is finished without them).

`;
s = s.replace(anchor, entry + anchor);
fs.writeFileSync(p, s.replace(/\n/g, NL));
console.log("CLAUDE.md: 1984 entry added");

Drop the seventeen panel decals here, with the exact filenames from the parts
order (docs/parts-order.html). Nothing here is wired in until the file exists —
the panel falls back to the CSS version for anything missing, so partial
deliveries are safe.
